# 统一行尾：仓库内一律 LF，避免 Windows 上出现整文件 diff
* text=auto eol=lf

# 二进制/生成物不做行尾转换
*.db binary
*.db-wal binary
*.db-shm binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件与 Markdown 的 diff 噪音控制
package-lock.json -diff -text
*.md text eol=lf
